# program to print numbers from 1 to 10 using recursion

# recursive function that will accept n till we reach 10
def print1to10(n):
    # base case to stop the recursion
    if(n>10):
        return 
    print(n)
    
    # recursive all 1 -> 2, 2 -> 3, 3 -> 4 and so on
    print1to10(n+1)
    
print1to10(1)